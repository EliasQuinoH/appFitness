name: mobile
description: "A new Flutter project."

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  # === UI y Multimedia (Lottie, Audio) ===
  lottie: ^3.3.2
  audioplayers: ^6.5.1
  cupertino_icons: ^1.0.8
  # === Base de Datos Local (Drift) ===
  drift: ^2.29.0
  path_provider: ^2.1.2
  sqlite3_flutter_libs: ^0.5.21
  path: ^1.8.3 # Necesaria para usar 'path_provider' correctamente
  # === Red y Seguridad (Login y Sincronización) ===
  dio: ^5.4.3 # Cliente HTTP para Django API
  flutter_secure_storage: ^9.0.0 # Almacenamiento seguro de tokens JWT
  connectivity_plus: ^6.0.4 # Detección de estado offline/online

  google_mlkit_pose_detection: ^0.14.0
  camera: ^0.11.3
  google_mlkit_commons: ^0.11.0

  http: ^1.1.0 # Para hacer peticiones HTTP

dev_dependencies:
  # === Herramientas de Desarrollo ===
  flutter_launcher_icons: "^0.14.4"
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  # === Generación de Código ===
  drift_dev: ^2.29.0
  build_runner: ^2.4.9

flutter_icons:
  android: true
  ios: true
  image_path: "assets/images/icono_app.png"
  remove_alpha_ios: true

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/animations/
    - assets/audio/music/
